# Database Configuration
# استخدم أحد المتغيرين التاليين (DATABASE_URL أو POSTGRES_URL)
# للحصول على رابط الاتصال من Supabase:
# 1. افتح مشروعك في Supabase Dashboard
# 2. اذهب إلى Settings > Database > Connection String
# 3. اختر "URI" واستخدم Connection Pooling (Port 6543) للإنتاج

DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require
# أو
POSTGRES_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require

# OpenAI API Configuration
# للحصول على API Key:
# 1. سجل دخول إلى https://platform.openai.com/
# 2. اذهب إلى API Keys
# 3. أنشئ مفتاح جديد
OPENAI_API_KEY=sk-proj-...

# JWT Secret for Authentication
# يمكنك توليد مفتاح عشوائي باستخدام:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here-min-32-characters

# ElevenLabs API (اختياري - للتحويل النصي إلى صوت)
# ELEVENLABS_API_KEY=sk_...

# Vercel Blob Storage (اختياري - لرفع الملفات)
# BLOB_READ_WRITE_TOKEN=vercel_blob_...

